import XCTest
@testable import AVPlayer_SocketIO

final class AVPlayer_SocketIOTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AVPlayer_SocketIO().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
